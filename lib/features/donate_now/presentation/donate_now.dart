import 'package:flutter/material.dart';

class DonateNow extends StatelessWidget {
  const DonateNow({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("donate now")));
  }
}
